<h1><%= @article.title %></h1>
<p><%= @article.text %></p>

<h1>Comments</h1>
<% @article.comments.each do |comment| %>
<p><b><%= comment.commenter %> Says:</b></p>
<p><%= comment.body %></p>
<p><%= link_to "Delete comment", [comment.article, comment], method: :delete, data: {confirm: "Are  you sure you want to delete this comment?"} %></p>
<% end %>

<%= link_to "Delete article", article_path(@article), method: :delete, data: { confirm: "Are you sure" } %> | 
<%= link_to "Edit Article", edit_article_path, data: { confirm: "Are you sure you want to edit this article?" } %>

<%= render 'comments/commentform' %>

<%= link_to "Back to articles", articles_path %> 